# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# typescript
*.tsbuildinfo

# environment
.env
.env*.local

# vercel
.vercel

# package manager lockfiles — repo currently doesn't track these;
# add the specific lockfile here if the convention changes
package-lock.json
yarn.lock
pnpm-lock.yaml

# growth scripts — raw exports and generated feeds are local artefacts
/scripts/growth/data/raw/
/scripts/growth/data/processed/
/scripts/growth/data/output/

# os / editor
.DS_Store
*.pem
.idea/
.vscode/
